
import packageJson from '../../package.json';

export const Version = ({version}) => {
    return (
        <>
            <h5>Version {packageJson.version}</h5>
            
        </>
    );
};
   